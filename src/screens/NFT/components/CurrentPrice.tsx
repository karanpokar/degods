import { View, Text } from 'react-native'
import React from 'react'
import { colors } from '../../../utils/theme'

const CurrentPrice = ({listingData}:any) => {
  return (
    <View style={{backgroundColor:colors.secondaryBackground,borderRadius:12,marginVertical:16,flexDirection:'row',padding:12,justifyContent:'space-between',alignItems:'center'}}>

    </View>
  )
}

export default CurrentPrice